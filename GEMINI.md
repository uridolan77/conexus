# Gemini CLI Instructions

Read `AGENTS.md` first.

Then read the Conexus agent-os package docs in `docs/spdd-agent-os-starter/`, especially the profile, scope, architecture, provider abstraction, and REASONS docs.

Work in small, validated Conexus slices and avoid speculative platform expansion.
