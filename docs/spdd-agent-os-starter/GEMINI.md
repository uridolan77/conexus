# Gemini CLI Instructions

Read `AGENTS.md` first. It is the canonical project instruction file.

Then read `.agent-os/profile.yml` and relevant docs under `docs/product`, `docs/architecture`, and `docs/specs`.

Follow the SPDD workflow in `AGENTS.md`.
