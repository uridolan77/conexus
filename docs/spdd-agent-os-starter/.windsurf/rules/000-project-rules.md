# Windsurf Project Rules

Use `AGENTS.md` as the canonical project instructions.

Before coding, inspect `.agent-os/profile.yml` and relevant docs under `docs/product`, `docs/architecture`, and `docs/specs`.

Prefer small vertical slices. Validate before declaring success.
