---
applyTo: "**/*.{ts,tsx,js,jsx,css,scss,html}"
---

# Frontend Instructions

Follow `AGENTS.md`.

- Prefer accessible components.
- Keep UI state local unless shared state is justified.
- Avoid large component rewrites when a local change is enough.
- Preserve existing design language unless the task is design-system work.
